{
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ],
  "installCommand": "rustup target add wasm32-unknown-unknown && cargo install wasm-pack && pnpm install",
  "buildCommand": "wasm-pack build --target bundler --out-dir pkg && pnpm run build",
  "outputDirectory": "dist"
}